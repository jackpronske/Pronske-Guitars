services:
  - type: web
    name: pronske-guitars
    repo: https://github.com/jackpronske/Pronske-Guitars
    branch: main
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build-prod
    startCommand: node server
